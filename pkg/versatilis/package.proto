syntax = "proto3";

package versatilis;

option go_package = "../versatilis";

message Message {
    string stringMessage = 10;
    bytes bytesMessage = 20;
}

message Package {
    string version = 10;

    bytes noiseHandshakeInfo = 20;
    bytes noiseCiphertext = 23;
    bytes noiseAuthTag = 25;
}
